/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Static files in /public are served from the root; terminal.html lives there.
};

export default nextConfig;
